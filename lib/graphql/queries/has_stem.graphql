query HasStem(
    $stem: String!
    $targetLangs: [String]!
    $wantedDicts: [String]!
) {
    hasStem(
        exact: $stem
        targetLangs: $targetLangs
        wantedDicts: $wantedDicts
    ) {
        stem
    }
}