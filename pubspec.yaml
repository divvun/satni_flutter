name: satni
description: Sámi dictionary and terminology app
publish_to: "none" # Remove this line if you wish to publish to pub.dev
version: 0.1.0+1

environment:
  sdk: ">=2.17.0 <3.0.0"
  flutter: ^3.0.0

dependencies:
  flutter:
    sdk: flutter
  flutter_hooks: ^0.18.4
  freezed_annotation: ^2.0.3
  go_router: ^3.0.2
  graphql: ^5.0.0
  flutter_riverpod: ^1.0.4
  hooks_riverpod: ^1.0.3
  json_annotation: ^4.4.0

dev_dependencies:
  flutter_test:
    sdk: flutter

  flutter_lints: ^2.0.1
  build_runner: ^2.1.7
  freezed: ^2.0.3+1
  artemis: 7.7.0-beta
  json_serializable: ^6.1.3
  import_sorter: ^4.4.1

# The following section is specific to Flutter.
flutter:
  uses-material-design: true
dependency_overrides:
  async: 2.9.0
  characters: 1.2.1
  analyzer: 4.1.0

  # Tame the gql_* family to not use alpha releases
  gql_exec: 0.4.0
  gql_http_link: 0.4.2
  gql_link: 0.4.2
  gql_dedupe_link: 2.0.2
