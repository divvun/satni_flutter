name: satni
description: Sámi dictionary and terminology app
publish_to: "none" # Remove this line if you wish to publish to pub.dev
version: 0.1.0+1

environment:
  sdk: ">=2.17.0 <3.0.0"
  flutter: ^3.3.3

dependencies:
  flutter:
    sdk: flutter
  flutter_hooks: ^0.18.4
  flutter_riverpod: ^1.0.4
  freezed_annotation: ^2.0.3
  go_router: ^3.0.2
  hooks_riverpod: ^1.0.3
  json_annotation: ^4.4.0
  rxdart: ^0.27.4
  satni_graphql_service:
    path: packages/satni_graphql_service

dev_dependencies:
  build_runner: ^2.1.7
  flutter_lints: ^2.0.1
  flutter_test:
    sdk: flutter
  freezed: ^2.0.3+1
  import_sorter: ^4.4.1
  json_serializable: ^6.1.3

# The following section is specific to Flutter.
flutter:
  uses-material-design: true
# dependency_overrides:
