targets:
  $default:
    sources:
      - lib/**
    builders:
      artemis:
        options:
          schema_mapping:
            - schema: lib/src/graphql/schemas/schema.graphql
              queries_glob: lib/src/graphql/queries/*.graphql
              output: lib/src/graphql/api/graphql_api.dart
